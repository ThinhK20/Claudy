models/

# DirectML.dll is copied from the ort build cache by CI before bundling
# (see .github/workflows/release.yml); it is a build artifact, never committed.
src-tauri/DirectML.dll

# Local-only working directories (not published).
# Anchored to the repo root so docs/promo/ (the published video) still ships.
/promo/
/docs/superpowers/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
